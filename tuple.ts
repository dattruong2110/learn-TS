// Array
let arr = ['Dat', 22, true];
arr[0] = 28;
arr[1] = 'Dat';
arr[2] = 77;

// console.log(arr);

// Tuple giống array nhưng những phần tử bên trong phải có kiểu cụ thể ở những vị trí cụ thể
const tup: [string, number, boolean] = ['Dat', 22, true];

const myStudent: [string, number] = ['Dat', 22];